/*package service;
import dao.userDAO;
import model.user;

public class UserService {

    userDAO dao = new userDAO();

    public void register(user u) {
        dao.addUser(u);
    }

    public boolean login(String email, String tel) {
        return dao.checkLogin(email, tel);
    }
}*/